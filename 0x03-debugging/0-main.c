#include "main.h"

/**
* main - Start here
* Desc - Based on the main.c file above, create a file named 0-main.c.
* This file must test that the function positive_or_negative() gives the
* correct output when given a case of 0.
* Return: 0
*/

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
